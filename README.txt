PSEUDCODE - CONNECT 4


1) Define apps’s state (variables)
        1.1) board
        1.2) players (turn)
        1.3) gamestatus


2) visual representation (index) board 
        2.1) 7x6 = 42 squares
        2.2) blue / red (p1 / p2)
	2.3) yellow border ( game board)


3) Store elements on the page that will be accessed in code more than once in variables to make code more concise, readable and performant.


4) Upon loading the app should:
        4.1) Initialize the state variables
        4.2) Render those values to the page
        4.3) Wait for the user to click a square


5) Handle a player clicking a square (players Turn)
        5.1) fill space after player turn

6) Handle a player clicking the replay button (finished?)


7) GameStatus
        7.1) Winner / Tie?
        7.2) Play Again








​​